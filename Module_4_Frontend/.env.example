# Base URL of the RentNest API. Requests to /api/* on this app are proxied here
# so authentication cookies work same-origin with zero CORS friction.
NEXT_PUBLIC_API_URL=https://rent-nest-api-seven.vercel.app